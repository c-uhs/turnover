\PassOptionsToClass{rgb}{xcolor}
\usepackage{tikz}
\usepackage{tikz-qtree}
\usetikzlibrary{positioning,arrows.meta,quotes,calc}
\tikzset{%
  arrow/.style    = { ->, >=Latex,  very thick, rounded corners, fill = #1, draw = #1 },
  tbox/.style     = { fill = #1!20!white, draw = #1!80!white, thick, align = center,
                      minimum width = 0.8cm, minimum height = 0.6cm, node distance = 1.5cm },
  branch/.style   = { fill = gray!20!white, draw = gray!80!white, thick, align = center, rounded corners = 0.1cm, grow = down,
                      minimum width = 1.2cm, minimum height = 0.5cm, node distance = 1cm },
  leaf/.style     = { fill = blue!20!white, draw = blue!80!white, thick, align = center, rounded corners = 0.3cm, grow = down,
                      minimum width = 0.6cm, minimum height = 0.6cm }
}

\input{figs/tikz/tikz-colors.tex}

\newcommand{\connectall}[2]{
  \foreach \s in {#1} {
    \foreach \t in {#2} {
      \draw[arrow,<->] (\s) -- (\t);
    }
  }
}